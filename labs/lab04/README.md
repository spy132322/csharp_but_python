# Лаба 4

### 1. Интерфейс `Printable`

**Контракт:** объект может быть представлен в виде строки.

```python
from abc import ABC, abstractmethod

class Printable(ABC):
    @abstractmethod
    def to_string(self) -> str:
        """Возвращает строковое представление объекта."""
        pass
```

### 2. Интерфейс Comparable

Контракт: объект может сравниваться с другим объектом.

```python
class Comparable(ABC):
    @abstractmethod
    def compare_to(self, other: object) -> int:
        """
        Сравнивает текущий объект с other.
        Возвращает:
        < 0 – если текущий объект меньше,
        0   – если объекты равны,
        > 0 – если текущий объект больше.
        """
        pass
```

### 3. Реализация интерфейсов в классах

Интерфейсы Printable и Comparable реализованы в базовом классе Product (из лабораторной работы №3), а также во всех его наследниках:

- LiquidProduct – жидкий продукт (учитывает объём и тип тары);

- TangibleProduct – материальный товар (учитывает вес и логистику);

- ServiceProduct – услуга (учитывает часы работы и сложность).

### 4.Работа с коллекцией ProductCatalog

    get_printable() – возвращает только те элементы, которые реализуют Printable.

    get_comparable() – возвращает элементы, реализующие Comparable.

Сортировка через интерфейс

Метод sort_by_comparable() сортирует коллекцию, используя compare_to() тех элементов, которые поддерживают этот интерфейс. Элементы, не реализующие Comparable, исключаются из сортировки или помещаются в конец (в зависимости от реализации).

### demo.py

#### Cценарий 1. Базовые возможности интерфейсов

- Создание объектов разных типов (LiquidProduct, TangibleProduct, ServiceProduct).

- Полиморфный вывод всех объектов через Printable.to_string().

- Проверка типов с помощью isinstance().

- Сравнение объектов через Comparable.compare_to().

![aa](/labs/lab04/images/Screenshot_20260428_120856.png)

#### Сценарий 2. Работа с каталогом продуктов

- Наполнение ProductCatalog разнородными объектами.

- Фильтрация каталога по интерфейсам (get_printable(), get_comparable()).

- Сортировка каталога через sort_by_comparable().

![aa](/labs/lab04/images/Screenshot_20260428_120923.png)

#### Сценарий 3. Расширенная бизнес-логика

- Фильтрация товаров по активности, доступности, высокой цене.

- Поиск товара по имени.

- Операция «покупка» товара (изменение остатков или статуса).

- Сортировка отфильтрованного списка.

![aa](/labs/lab04/images/Screenshot_20260428_120951.png)